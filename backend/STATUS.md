# Backend status

Status at checkpoint: **NOT_FOUND**.

The checked phone roots contained no verified Movia media-parser source, catalog runtime database, or running Movia backend process. This directory contains status/documentation only; it is not a substitute implementation.

Expected future contents:

- media-parser source;
- catalog/search/discovery;
- playback resolver and P2P logic;
- service definition;
- tests;
- exact runtime path and restore procedure.

Do not claim backend health until a real service responds and its source/checksum is recorded in `CURRENT_BASELINE.json`.
