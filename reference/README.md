# External reference data

The current Zona reference is kept outside the canonical source tree:

    /data/data/com.termux/files/home/projects/zona-reference-20260829-223618

The user-facing storage directory /storage/emulated/0/Movia/Zona contains raw
captures and is intentionally not copied into this project. This directory
contains a manifest link only so recovery is unambiguous and no large raw
reference data is duplicated.
