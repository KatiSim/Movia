# [ADR-XXX] Название задачи / Версия (vX.X.X, build XXX)
**Дата:** YYYY-MM-DD HH:MM

### 1. Проблема (Problem Statement)
Краткое описание симптома: что именно сломалось, зависло или отсутствовало в UI/бэкенде.

### 2. Первопричина (Root Cause)
Техническая причина сбоя на уровне кода/сети (таймауты сокетов, Range-запросы, CORS, блокировка Anti-Leech, пропущенные поля в SQLite).

### 3. Решение (Solution & Architecture)
Сжатое описание внесенных правок:
- Что изменено в архитектуре.
- Какие модули и функции переписаны.

### 4. Измененные файлы (Changed Files)
- `path/to/file1.kt`
- `path/to/file2.py`

### 5. Использованные инструменты и команды (Tools & Verification)
Список утилит и команд, использованных для диагностики, сборки и проверки:
```bash
# Команды диагностики и проверки (curl, logcat, dumpsys, sqlite3, tshark)
# Команда сборки и деплоя через Shizuku (rish)
```
