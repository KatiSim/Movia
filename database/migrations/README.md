# Catalog migrations

The current phone inventory did not contain a verified Movia catalog database or migration set. Add each future schema migration here, in order, and record its checksum in the baseline manifest.

The large catalog database itself must remain a release/backup artifact, not ordinary Git history.
